# 开启 Mock 数据模式（用于前端调试）
# 设置为 true 时，将使用本地 mock 数据而不是调用后端 API
VITE_USE_MOCK=false

# 后端 URL（当 VITE_USE_MOCK=false 时使用）
VITE_BACKEND_URL=http://localhost:8001

# 编辑器访问密码
# 请设置您自己的密码以保护文章编辑器
VITE_EDITOR_PASSWORD=your_password_here
